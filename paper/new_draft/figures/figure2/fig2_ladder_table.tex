% Table I of the paper (the Fig. 2 ladder numbers) as a standalone figure, so it
% can be combined with the Fig. 2 artwork in a vector editor.
%
%   latex fig2_ladder_table.tex && dvips -E -o fig2_ladder_table.eps fig2_ladder_table.dvi
%   epstopdf fig2_ladder_table.eps
%
% dvips -E (not pdflatex) keeps the glyphs as Type 1 text so Illustrator can
% still select them -- the same reason the other figures avoid alpha fills.
% Body only, no caption: the caption text stays in main.tex.
% Font, size and column spacing mirror main.tex's `table` block: ieeeconf sets
% \rmdefault to ptm (Times) at 10 pt, the table is \footnotesize, \tabcolsep 5pt.
\documentclass[10pt]{article}
\usepackage[T1]{fontenc}
\renewcommand{\rmdefault}{ptm}
\usepackage{amsmath}
\usepackage{booktabs, makecell}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tabular}
\setlength\PreviewBorder{1pt}
\pagestyle{empty}
\begin{document}
\footnotesize
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lcccc@{}}
  \toprule
  Panel & \makecell{Constraint level\\(\% of $U_\mathrm{ref}$)}
        & \makecell{Terminal\\bound $\bar{u}$ (m)}
        & \makecell{Primary\\length (m)}
        & \makecell{Terminal\\$\sigma$ (m)} \\
  \midrule
  (a) & 100 & 13.75 & 1741 & 9.73 \\
  (b) &  70 &  9.62 & 1761 & 8.89 \\
  (c) &  50 &  6.87 & 1822 & 6.66 \\
  (d) &  30 &  4.12 & 1890 & 3.91 \\
  \bottomrule
\end{tabular}
\end{document}
