I have rearranged your document to better structure it for parsing. Note a couple of things: 

1. There are multiple "sections" that correspond to each of the sections listed in your paper. You may need to reorganize these if you plan to structure the paper slightly differently.

2. Each "section" is structured as follows:
    1. sections
        0. sec0\_name1 --> section top-level for the "zeroth" section (abstract) called "name1"
            1. figures/ --> This is where you put the figures for the corresponding section (zero)
            del\_abs\_instruct.tex --> This is where you put the section text
        1. sec1\_name2 --> section top-level for the "first" section (abstract) called "name2"
            1. figures/ --> This is where you put the figures for the corresponding section (zero)
            intro.tex --> This is where you put the section text
            intro2.tex --> If you have too long of a file, break out into a second section.
        ...

Notes: Even if an #include line is somewhere else in the document to display a graphic, always put the reference graphic in the correct folder.

%%%%%% IMPORTANT THINGS TO NOTE %%%%%%%%%%%%%%%
ARXIV STORES THE ENTIRE OVERLEAF CODE IF YOU PROVIDE IT. THIS MEANS THAT "outline.text" WILL BE AVAILABLE. YOU MUST DELETE THESE BEFORE YOU SUBMIT AND REVIEW ANY COMMENTS YOU HAVE. I LIKE TO DENOTE FILES THAT I WANT TO DELETE WITH SOME SORT OF MARKER (del), which I then cntl+f to get rid of.

TODOS:

1. REMOVE ALL FILES WITH "del" in structure.
2. REMOVE ALL COMMENTS WITH "[del]" prefacing the comment.
3. BUILD FROM SCRATCH BEFORE YOU SEND IN THE DOCUMENT. (This makes sure all dependencies are there, RECOMPILE 
        